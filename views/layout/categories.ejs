<!-- Start Categories -->

        <% categories.forEach(category => { %>
                    <a class="btn btn-success" href="/category/<%= category.id %> ">
                        <%= category.name %>
                    </a>
        <% }) %>
    
<!-- End Categories -->